---
import "../styles/global.css";
import editIconGreen from "../assets/edit_green.svg";
import editIconMuted from "../assets/edit_muted.svg";

interface Props {
  unit?: string;
  activeFrom?: string;
  location?: string;
  price?: string;
  nextBillingDate?: string;
  card?: string;
  nextPaymentStatus?: string;
}
---

<div
  class="active-unit bg-light-bg border border-solid border-light-muted p-6 mb-6 mx-6"
>
  <h2 class="font-display text-4xl font-black pb-4">
    {Astro.props.unit || "N/A"}
  </h2>
  <p class="font-mono pb-2">
    Active From: <b>{Astro.props.activeFrom || "N/A"}</b>
  </p>
  <p class="font-mono pb-2">Location: <b>{Astro.props.location || "N/A"}</b></p>
  <p class="font-mono pb-2">Price: <b>{Astro.props.price || "N/A"}</b></p>
  <p class="font-mono pb-2">
    Next Billing Date: <b>{Astro.props.nextBillingDate || "N/A"}</b>
  </p>
  <p class="font-mono pb-2">Card: <b>{Astro.props.card || "N/A"}</b></p>
  <p class="font-mono pb-2">
    Next Payment Status: <span class="text-brand underline underline-offset-4"
      ><b>{Astro.props.nextPaymentStatus || "N/A"}</b></span
    >
  </p>
  <div class="flex gap-2 justify-end pt-3">
    <img src={editIconMuted.src} alt="icon make change" />
    <p class="font-sans text-light-muted italic text-base">Make Change</p>
  </div>
</div>
